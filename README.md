# danialazzarini.github.io

Istruzioni:
- le pagine sono in `_pages/` (ad esempio `_pages/talks.html`, ...)
- le collezioni sono caricate nelle pagine da `_data`; ad esempio
  `site.data.workshop` legge da `_data/workshop.yml`
- il menu è in `_data/navigation.yml`
